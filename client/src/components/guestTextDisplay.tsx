import "../styles/guestTextDisplay.css"

const guestTextDisplay:React.FC = () => {
    return (<div className="guest-text-container" >
        <p>Vous êtes connecté(e) en tant qu'invité(e).</p>
    </div>)
}

export default guestTextDisplay;